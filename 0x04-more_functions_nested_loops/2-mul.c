#include "main.h"

/**
 * mul - multiplies the digit input
 * @a: the number checked
 * @b: the number checked
 * Return: returns 0 or 1
 */

int mul(int a, int b)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
